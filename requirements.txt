# Core orchestration
langgraph>=0.2.0

# LLM + image generation
anthropic>=0.30.0
openai>=1.40.0

# HTTP client
httpx>=0.27.0
requests>=2.31.0

# DuckDuckGo search — used to find Etsy listings without Cloudflare blocks
ddgs>=0.1.0

# Image processing
Pillow>=10.4.0

# Environment / config
python-dotenv>=1.0.0

# sqlite3 is part of the Python standard library — no install needed
